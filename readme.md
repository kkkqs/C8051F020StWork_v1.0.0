#### Todo
1. **电梯**在`X`层楼，**外侧**在`X`层楼时，此时**外侧**按下`上`或者`下`键时，**电梯**应该响应，并且**开门**
2. LCD 显示偶尔会熄灭，LED数码管在**电梯**停靠在`X`层楼时，讲显示的`OP`换为`FL`
3. **电梯**在`X`层楼，停靠，**内侧**按**开门**或者**关门**键无反应。当且仅当**电梯**在`X`层楼，刚刚抵达，正在**开门**或者**关门**时，按下**开门**或者**关门**键会改变**开门**或者**关门**的状态。
4. **电梯**在`X`层楼，**外。侧**在`Y`层楼时，若`X<Y`，则**外侧**在输入楼层之后只能按`下`；反之，若`X>Y`，则**外侧**在输入楼层之后只能按`上`。
5. 关于楼层选择停靠的决策算法，由`tr`的想法进行修改。
6. 加入存储密码和3个配置参数，并能进行修改的功能。
7. **蜂鸣器的小曲**
8. 8个LED灯

---
#### 内容分配

1. 赵文昊：
   - 整体状态机框架
   - 程序架构
   - runningTask $\frac{1}{6}$调度算法
   - 蜂鸣器
   - 8个LED灯
2. 王若然：
   - 中间层
   - runningTask $\frac{1}{3}$
   - inputTask
3. 戴圣轩：
   - 驱动：led和lcd
   - runningTask $\frac{1}{3}$
4. tr
   - 驱动：定时键盘
   - runningTask $\frac{1}{6}$调度算法





> 1. **电梯**在`X`层楼，**外侧**在`X`层楼时，此时**外侧**按下`上`或者`下`键时，**电梯**应该响应，并且**开门**
> 2. LCD 显示偶尔会熄灭，LED数码管在**电梯**停靠在`X`层楼时，讲显示的`OP`换为`FL`
> 3. **电梯**在`X`层楼，停靠，**内侧**按**开门**或者**关门**键无反应。当且仅当**电梯**在`X`层楼，刚刚抵达，正在**开门**或者**关门**时，按下**开门**或者**关门**键会改变**开门**或者**关门**的状态。
> 4. **电梯**在`X`层楼，**外。侧**在`Y`层楼时，若`X<Y`，则**外侧**在输入楼层之后只能按`下`；反之，若`X>Y`，则**外侧**在输入楼层之后只能按`上`。

由戴圣轩和王若然解决吧

> 关于楼层选择停靠的决策算法，由`tr`的想法进行修改。

TR

> 加入存储密码和3个配置参数，并能进行修改的功能。

谁负责Configuration谁来

> 蜂鸣器的小曲
>
> **蜂鸣器的小曲**
>
> 8个LED灯

赵文昊